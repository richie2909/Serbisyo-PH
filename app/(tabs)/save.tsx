import { View } from "react-native";

const savedData = [
  { id: "1", title: "Saved Item", subtitle: "Saved for later" },
];

export default function SaveScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: "#f9f9f9" }}>
    </View>
  );
}
